---
// Henter parametrene fra Astro.props
const {
  htmlheading = "h1",
  size = "h1",
  font = "",
  stylingclasses = "",
} = Astro.props;
const Htmlheading = htmlheading;

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
  h1: "text-[35px] lg:text-[50px] font-semibold leading-[56px]",
  h2: "text-[30px] leading-[42px] lg:text-[38px] font-medium lg:leading-[45px]",
  h3: "text-[17px] lg:text-[25px] font-medium",
  h4: "text-[15.5px] lg:text-[20px] font-semibold",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {};
---

<Htmlheading class:list={[stylingclasses, sizeClasses[size], fontClasses[font]]}
  ><slot /></Htmlheading
>
